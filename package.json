{
  "name": "emergency-cert-takeover",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:server": "npm --workspace server run start:dev",
    "dev:web": "npm --workspace web run dev",
    "build": "npm --workspace server run build && npm --workspace web run build",
    "test": "npm --workspace server run test"
  },
  "workspaces": [
    "server",
    "web"
  ],
  "devDependencies": {
    "@electric-sql/pglite": "^0.2.12",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
